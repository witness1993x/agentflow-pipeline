# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
build/
dist/*.whl
dist/*.tar.gz
.eggs/
.venv/
venv/
env/
.python-version

# Tests / coverage
.pytest_cache/
.coverage
htmlcov/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Host-project state (these are generated artefacts of using the framework, not framework source)
cases/HSP-*/
workspaces/HSP-*/
trends/
.agentflow.toml

# Skill bundle: track skill/ + dist/*.zip but never the freshly-rebuilt egg-info inside bundle/
skill/bundle/src/agentflow_pipeline.egg-info/
skill/bundle/**/__pycache__/

# Local secrets / env
.env
.env.local
*.local
